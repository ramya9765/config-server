spring:
  h2:
    console:
      enabled: true
  application: Department-Service
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
 
